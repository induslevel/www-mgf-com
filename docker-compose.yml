services:
  astro-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: MGF-com
    ports:
      - "4333:4323"
    restart: unless-stopped
    networks:
      - frontend
    environment:
      - USE_CLOUDFLARE=false
      - NODE_ENV=production
      - SITE_URL=https://mgf.solocarbon.com

networks:
  frontend:
    external: true
